export default class LevelFinder {
    constructor(data) {
        this.data = data
        this.images = []
        let rand = Math.floor(Math.random()*data.D1.length)
        this.images.push(data.D1[rand1])
        rand = Math.floor(Math.random()*data.D2.length)
        this.images.push(data.D2[rand1])
        rand = Math.floor(Math.random()*data.D3.length)
        this.images.push(data.D3[rand1])
    }

}